/**
 * 소켓 세션 매핑 관리 파일
 * - 각 사용자의 실시간 웹소켓 연결 상태(사용자 ID ↔ 소켓 ID)를 저장하는 객체를 제공
 * - 여러 소켓 핸들러(알림, 채팅 등)에서 공유하여 사용자별로 메시지/알림을 정확히 전달할 수 있음
 * - 소켓 연결/해제 시 해당 객체를 업데이트하여 관리
 */
module.exports = {
    notificationUsers: {},
    chatUsers: {}
};
